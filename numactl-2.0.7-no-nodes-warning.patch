diff -up numactl-2.0.7/libnuma.c.orig numactl-2.0.7/libnuma.c
--- numactl-2.0.7/libnuma.c.orig	2011-04-14 18:23:06.000000000 +0200
+++ numactl-2.0.7/libnuma.c	2012-02-15 11:45:03.497203735 +0100
@@ -1283,6 +1283,8 @@ numa_node_to_cpus_v1(int node, unsigned 
 		numa_warn(W_nosysfs2,
 		   "/sys not mounted or invalid. Assuming one node: %s",
 			  strerror(errno));
+		numa_warn(W_nosysfs2,
+		   "(cannot open or correctly parse %s)", fn);
 		bitmask.maskp = (unsigned long *)mask;
 		bitmask.size  = buflen_needed * 8;
 		numa_bitmask_setall(&bitmask);
